@model Bolzplatzarena.Blog.Models.StandardPage
@{
    ViewBag.Title = Model.Title;
}
<main>
    @if (Model.NavigationTitle != "Startseite")
    {
        <h1>@Model.Title</h1>   
    }
    @Html.DisplayFor(m => m.Blocks)
</main>